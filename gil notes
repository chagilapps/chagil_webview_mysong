InAppWebView

NOTE: To use it on iOS, you need to opt-in for the embedded views preview by adding a boolean
property to the app's Info.plist file, with the key io.flutter.embedded_views_preview and the value YES.

flutter downloader #
Enable background mode
Add sqlite library
Configure AppDelegate
Support HTTP request? disable Apple Transport Security (ATS) feature